package com.philipcosgrave.calorietracker.data.local

/**
 * Room stores syncable records and outbox items.
 * DataStore stores device id, sync cursor, and user sync settings.
 *
 * This package is intentionally scaffold-only for now so the app can compile
 * while the UI continues using the existing local persistence flow.
 */
internal object LocalStorageScaffold
